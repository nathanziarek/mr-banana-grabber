exports.rate = function (pixelCounts, _callback) {
    for( key in pixelCounts ){
        // build array of ab colors
        // build a mao of ab colors back to rgb colors
    }
    _callback(returnObj);
}